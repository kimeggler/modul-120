<UserControl x:Class="M120Projekt.TopDownNavigation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:M120Projekt"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Button x:Name="back" Grid.Column="0">Zurück</Button>
        <Button x:Name="newbutton" Grid.Column="1">Neu</Button>
        <Button x:Name="save" Grid.Column="2">Speichern</Button>
        <Button x:Name="editButton" Grid.Column="3">Bearbeiten</Button>
        <Button x:Name="delete" Grid.Column="4">Löschen</Button>

    </Grid>
</UserControl>
